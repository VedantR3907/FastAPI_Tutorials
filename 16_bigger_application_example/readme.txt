1. This is just and example that how might the bigger application might look.
2. The main.py is the main file where everything will be called.
3. dependencies.py where all the dependencies are written.
4. routers is the folder where different api's are written seperately (Like for students we have different API, for teacher different)
5. Database folder contains 2 different json student and teachers database json where we will store the entry of teacher and student.